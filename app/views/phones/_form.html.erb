<%= simple_form_for(@phone) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :phone_no %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
